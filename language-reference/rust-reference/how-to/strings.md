# Iterate over lines of a multi-line string

```rs
for line in contents.lines() {
    // do something with line
}
```

# Check whether a string contains another string

```rs
if line.contains(query) {
    // do something with line
}
```

