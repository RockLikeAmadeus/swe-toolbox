# Modes

Normal Mode (`Esc`)
Insert Mode (`i`, `a`, `o`)
Replace Mode (`r`, `R`)
Visual Mode (`v`, `V`, `Ctrl+v`)
Command Mode (`:`)

# Critical Combinations

## Exiting Vim

Discard changes and exit: `:q!`

# Cursor Movement

Left: `h`
-> `l`
^ `k`