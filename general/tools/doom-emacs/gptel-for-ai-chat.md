# Using gptel for querying LLMs in Doom Emacs

With my current config set up as it is, you simply need to press `SPC o a` (space open ai), select the model, and type your query on the level three heading in the markdown buffer that opens automatically.

Execute queries with `C-c RET`.

